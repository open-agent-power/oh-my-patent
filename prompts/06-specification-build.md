# 06 · 申请文件生成

## 执行门禁

- **前置**：已按 `01-intake.md` 定下专利类型。
- **先 Read**：`docs/format-spec.md`（版面与形式的完整规范）。
- **禁止**：手工拼 `.docx`。格式的正确性归脚本，内容的正确性归你。
- **禁止**：为了让「格式好看」去改脚本产出的 XML。格式问题一律通过命令行选项调整，
  选项不够用就改 `oh_my_patent/spec.py`。
- **禁止**：跳过本文件末尾的「交付前自查」直接交付。

## 第 1 步 · 生成骨架（可选但推荐）

```bash
python tools/patent.py template <invention|utility|design> -o 稿件.md
```

骨架里所有需要填的位置都标了「待补充」。照抄结构、替换内容，比从空白页写快得多。

## 第 2 步 · 撰写正文

写入稿件文件（`outputs/` 下，或直接给出内容让用户保存）。

### 撰写要点

以下都是法规硬要求，不是风格偏好。脚本只能检查，不能替你写对。

#### 发明名称

- 应清楚、简要地反映要求保护的主题和类型，与请求书中的名称一致
- 一般不超过 25 字，特殊情况不超过 40 字
- **不得含**：人名、地名、商标、型号、商品名称；「专利」「新型」「改进」等非技术用语
- **不得使用**标点（必要的括号除外）
- 形状应为「一种……方法」/「一种……装置」/「一种……结构」

#### 权利要求书

- 编号用阿拉伯数字顺序编号：`1.` `2.` `3.`
- **第一项必须是独立权利要求**，且写在所有从属权利要求之前
- 独立权利要求应包含**前序部分**（主题名称 + 与最接近现有技术共有的必要技术特征）
  和**特征部分**（`其特征在于` + 区别技术特征）
- 从属权利要求开头写「根据权利要求X所述的……」，**只能引用在前的权利要求**
- 多项从属权利要求只能以择一方式引用
- **不得使用**「最好是」「例如」「尤其是」「必要时」「大约」「左右」等含义不确定的用语
- 除绝对必要外，不得使用「如说明书……部分所述」「如图……所示」
- **不得有插图**（化学式、数学式可以）

关于独立权利要求的数量：法条说「一项发明应当只有一个独立权利要求」，但
**符合单一性时可以写两项以上**（第一独权 + 并列独权）。《审查指南》第二部分
第六章 2.2.1 列出六种允许组合，最常用的是「方法 + 为实施该方法而专门设计的
设备」。写双独权是合法的，自检只会给 `WARNING` 提示复核单一性。

#### 说明书

五部分按**法定顺序**排列，顺序错了是硬错误：

```
【技术领域】 → 【背景技术】 → 【发明内容】 → 【附图说明】 → 【具体实施方式】
```

- **技术领域**：一句话写清本发明所属或直接应用的技术领域
- **背景技术**：写明对发明的理解、检索、审查有用的背景技术，并**指出现有技术
  存在的缺陷**。不要贬低他人产品，客观陈述
- **发明内容**：写清三件事——要解决的技术问题、技术方案、有益效果。
  技术方案应与独立权利要求一致，但可以写得更完整
- **附图说明**：逐幅写明「图1为……示意图」。附图要有标号
- **具体实施方式**：**这是说明书最重要的部分**。必须详细到「本领域技术人员
  能够实现」的程度。只给一个实施例时，权利要求的保护范围就很容易被限缩。
  建议给出多个实施例以支撑上位概括

引用的图号必须真实存在。正文提到「图2」就必须有图2。

#### 涉及人工智能模型或算法时

这是 2026-01-01 施行的新指南重点强化的部分，也是当前最容易被指出缺陷的一类。
除了通用要求，还要额外做到三点：

**1. 公开到「本领域技术人员能够实现」的程度**

- 涉及模型的**构建或训练**：写清模型的必要模块、层级或连接关系、训练步骤、
  必要的参数（层数、卷积核尺寸、学习率范围等）
- 涉及模型的**应用**：写清算法如何与具体场景结合、输入数据是什么、
  输出数据是什么、输入与输出之间为何构成这种映射关系

只写「采用神经网络提高准确率」远远不够。审查员要看到这条完整的链：

```
数据 → 预处理 → 模型结构 → 参数/训练 → 推理 → 后处理 → 技术效果
```

**2. 算法特征与技术特征要整体呈现，不能割裂**

不要把方案写成「算法部分归算法，硬件部分归硬件」。指南要求整体判断方案
是否利用技术手段、解决技术问题、获得技术效果。撰写时让算法特征与具体
技术场景**互相咬合**，而不是各自并列堆一段。

**3. 数据来源与伦理合规**

训练数据的采集方式必须合法（当事人同意、去标识化等）。**不得**以性别、
年龄、种族等个人属性作为决策依据——指南新增的审查示例明确把这类方案
排除在授权范围之外。

脚本会自动跑 `AI-001`～`AI-006` 与 `ETH-001`～`ETH-003` 覆盖上述要点。
但要注意自检的边界：它只能判断「有没有写」，不能判断「写得对不对」。
关键词命中不等于公开充分。

#### 摘要

- 写明技术问题、技术方案要点、主要用途
- **不超过 300 字**（含标点）
- **不得使用商业性宣传用语**：「最佳」「首创」「领先」「填补空白」等一律不能用
- 有附图时必须指定一幅摘要附图（不指定则取编号最小的那幅）

#### 实用新型的额外限制

这两条是最常见的实质缺陷，务必检查：

1. **必须有附图**。说明书应当有表示产品形状、构造或其结合的附图，不能省略
2. **只能有产品权利要求**。方法、工艺、用途类的主题实用新型保护不了，
   应改为申请发明专利

#### 外观设计的简要说明

应当写明（脚本会按法定顺序编号输出）：

1. 产品名称
2. 产品用途
3. **设计要点**（通常在于产品的形状）
4. 最能表明设计要点的图片或照片
5. 必要时：省略视图的情况、是否请求保护色彩

### 稿件格式

带 YAML frontmatter 的 Markdown：

```markdown
---
title: 一种基于边缘计算的智能水表数据采集方法
type: invention
applicant: 示例科技有限公司
inventors: [张三, 李四]
---

# 权利要求书

1. 一种……，其特征在于，……
2. 根据权利要求1所述的……，其特征在于，……

# 说明书

## 技术领域
……

## 背景技术
……

## 发明内容

### 要解决的技术问题
……

### 技术方案
……

### 有益效果
……

## 附图说明
图1为……示意图。

## 具体实施方式
参照图1，……

# 摘要
……
```

附图用 Markdown 图片语法，路径可以是本地文件：

```markdown
# 附图

![图1](图1.png)
![图2 摘要附图](图2.png)
```

或在 frontmatter 里结构化声明（外观设计常用）：

```yaml
drawings:
  - {number: 1, caption: 主视图, path: 主视图.png}
  - {number: 7, caption: 立体图, path: 立体图.png, abstract: true}
```

解析层很宽容：`【技术领域】`、`一、技术领域`、`技术领域：` 都认；权利要求
不带编号时按空行分段；无法归类的章节会保留而不是丢弃。

### 命令行参考

```bash
python tools/patent.py info     稿件.md            # 确认稿件被正确理解
python tools/patent.py lint     稿件.md            # 形式合规自检
python tools/patent.py build    稿件.md -o 出.docx  # 生成申请文件
python tools/patent.py template design -o 稿件.md   # 生成骨架
```

版面选项（`build` 可用）：

| 选项 | 说明 |
| --- | --- |
| `--font 宋体\|仿宋\|黑体` | 正文汉字字体，默认宋体（指南只允许这三种） |
| `--font-size 12` | 正文字号（磅），需落在 0.28~0.5cm 字高范围内 |
| `--line-spacing 1.5` | 行距倍数 |
| `--subsection-style inline\|keep\|flatten` | 「发明内容」三级小标题的处理方式。**默认 `inline`**，把「技术方案：」这类小标题并入段落做加粗引导词，最接近代理师的正式稿 |
| `--bold-sections` | 章节标题加粗（默认不加粗） |
| `--split` | 每部分独立成文件 |
| `--page-numbers` | 页脚页码 |
| `--strict` | 自检有错误则拒绝生成 |
| `--type` | 覆盖稿件的专利类型 |

`lint` 加 `--json` 可输出结构化结果，便于在流程里判断。

## 第 3 步 · 先自检，再生成

```bash
# 先看自检结果，根据 ERROR 逐条修正
python tools/patent.py lint 稿件.md

# 自检干净后再生成；--strict 会在有错误时拒绝出文件
python tools/patent.py build 稿件.md -o outputs/申请文件.docx --strict
```

**务必先 `lint` 再 `build`。** 直接 build 会把错误带进最终文件。
自检的读法与修正流程见 `07-self-check.md`。

需要拆分提交时：

```bash
python tools/patent.py build 稿件.md -o outputs/ --split --page-numbers
```

国知局的电子申请要求各部分**独立成文件、独立编页**，`--split` 就是对应这个场景的。

## 交付前自查

- [ ] 稿件里没有残留的「待补充」「XXX」一类占位符（`TXT-001` 会拦）
- [ ] 修正过的地方**重新跑过一遍 `lint`**，不是假设改对了
- [ ] 自检里发现的「是否合格」类问题，已经改到稿件里，而不是只写在报告里
- [ ] **自检清单本身没有写进申请文件正文**。正文里出现「自检」二字即不合格

## 下一步

进入 `07-self-check.md`。

---
状态：**已实现**。
